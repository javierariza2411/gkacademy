'use client';import Link from'next/link';import{useState}from'react';import{useRouter}from'next/navigation';import{api}from'@/lib/api';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('token',d.accessToken);localStorage.setItem('user',JSON.stringify(d.user));window.dispatchEvent(new Event('auth-change'));r.push('/dashboard')}catch(e:any){setMsg(e.message)}}return<div className="authWrap"><form className="authCard form" onSubmit={submit}><div className="kicker">Bienvenido</div><h2>Iniciar sesión</h2>{msg&&<p>{msg}</p>}<div className="field"><label>Correo</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Contraseña</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn">Ingresar</button><p className="muted">¿No tienes cuenta? <Link href="/registro">Regístrate</Link></p></form></div>}
